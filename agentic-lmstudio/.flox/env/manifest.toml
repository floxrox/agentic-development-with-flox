schema-version = "1.11.0"

[install]
# LM Studio
#lmstudio.pkg-path = "flox/lmstudio"
#lmstudio.pkg-group = "lmstudio"
lmstudio.store-path = "/nix/store/y5qi0d100k65f3pmfb05ip1kw39ynh3w-lmstudio-0.4.10-1+9678129"

# general-purpose tools
bat.pkg-path = "bat"
bat.pkg-group = "tools"
bat.outputs = "all"
curl.pkg-path = "curl"
curl.pkg-group = "tools"
curl.outputs = "all"
jq.pkg-path = "jq"
jq.pkg-group = "tools"
jq.outputs = "all"

# agentic cli tools
claude-code.pkg-path = "claude-code"
claude-code.pkg-group = "agentic"
claude-code.outputs = "all"
codex.pkg-path = "codex"
codex.pkg-group = "agentic"
codex.outputs = "all"
opencode.pkg-path = "opencode"
opencode.pkg-group = "agentic"
opencode.outputs = "all"
#openclaw.flake = "github:barstoolbluz/openclaw-nix"	# uncomment to use openclaw

# flox mcp server
flox-mcp-server.pkg-path = "flox/flox-mcp-server"
flox-mcp-server.systems = ["aarch64-darwin", "x86_64-linux"]
flox-mcp-server.pkg-group = "mcp"
flox-mcp-server.outputs = "all"

[vars]
# Server configuration - override at activation time
# LMS_PORT           - API server port (default: 1234)
# LMS_HOST           - API server bind address (default: 127.0.0.1)
# LMS_GUI            - Launch GUI on activation: true/false (default: false)
# LMS_CORS_ORIGIN    - CORS allowed origins (default: *)
# LMS_CONTEXT_LENGTH - Default context window for lms-launch auto-load (default: 131072)

[hook]
on-activate = '''
# === SERVER CONFIGURATION ===
export LMS_PORT="${LMS_PORT:-1234}"
export LMS_HOST="${LMS_HOST:-127.0.0.1}"
export LMS_GUI="${LMS_GUI:-false}"
export LMS_CORS_ORIGIN="${LMS_CORS_ORIGIN:-*}"
export LMS_CONTEXT_LENGTH="${LMS_CONTEXT_LENGTH:-131072}"

# Constructed base URL for helper functions
export LMS_API_BASE="http://${LMS_HOST}:${LMS_PORT}"

# === DATA DIRECTORIES ===
export LMS_LOG_DIR="$FLOX_ENV_CACHE/logs"
mkdir -p "$LMS_LOG_DIR"

# === GPU DETECTION ===
export _FLOX_ENV_CUDA_DETECTION=0
if command -v nvidia-smi >/dev/null 2>&1; then
  if nvidia-smi -L 2>/dev/null | grep -q "GPU"; then
    export _FLOX_ENV_CUDA_DETECTION=1
  fi
fi

# === DISPLAY CONFIGURATION ===
echo ""
echo "LM Studio + Agentic Tools environment ready"
echo ""
echo "Server:"
echo "  API: ${LMS_API_BASE}"
echo "  GUI: ${LMS_GUI}"
if [ "$_FLOX_ENV_CUDA_DETECTION" = "1" ]; then
  echo "  GPU: Available (CUDA detected)"
else
  echo "  GPU: Not detected (CPU mode)"
fi
echo ""
echo "Quick Start:"
echo "  flox activate -s                                  Start LM Studio server"
echo "  lms-launch claude --model zai-org/glm-4.7-flash   Download, load, launch"
echo ""
echo "  Context window: ${LMS_CONTEXT_LENGTH} (override: --context-length or LMS_CONTEXT_LENGTH)"
if [ "$_FLOX_ENV_CUDA_DETECTION" = "1" ]; then
  echo "  GPU: auto-detected (override: --gpu max|off)"
else
  echo "  GPU: not detected (override: --gpu max|off)"
fi
echo ""
echo "Commands:"
echo "  lms-launch <tool> --model <m> [args]      Download, load, and launch tool"
echo "  lms ls                                    List loaded models"
echo "  lmstudio-info                             Show configuration"
echo "  lmstudio-health                           Test API endpoint"
echo "  helpf                                     View full README documentation"
echo ""

# Fetch README.md if not present
README_FILE="$FLOX_ENV_PROJECT/README.md"
if [ ! -f "$README_FILE" ]; then
  if curl -fsSL https://raw.githubusercontent.com/floxrox/agentic-development-with-flox/main/agentic-lmstudio/README.md -o "$README_FILE" 2>/dev/null; then
    echo "Downloaded README.md (use 'helpf' to view)"
  fi
fi
'''

[services.lm-studio]
command = "lms-service"
is-daemon = false

[profile]
bash = '''
# Wait for API server if services are starting
if [ "${FLOX_ACTIVATE_START_SERVICES:-}" = "true" ]; then
  printf "Waiting for LM Studio API"
  _lms_ready=false
  for _i in $(seq 1 30); do
    if curl -sf "http://${LMS_HOST}:${LMS_PORT}/v1/models" >/dev/null 2>&1; then
      _lms_ready=true
      break
    fi
    printf "."
    sleep 2
  done
  if [ "$_lms_ready" = "true" ]; then
    echo " ready!"
  else
    echo " not responding (check: flox services logs lm-studio)"
  fi
fi

helpf() {
    local README_FILE="$FLOX_ENV_PROJECT/README.md"
    local README_URL="https://raw.githubusercontent.com/floxrox/agentic-development-with-flox/main/agentic-lmstudio/README.md"

    if [ "$1" = "--help" ]; then
        echo "Usage: helpf [OPTIONS]"
        echo ""
        echo "View environment documentation"
        echo ""
        echo "Options:"
        echo "  --force    Force download fresh copy from GitHub"
        echo "  --help     Show this help message"
        echo ""
        echo "The README is cached locally and only downloaded if missing."
        return 0
    fi

    if [ "$1" = "--force" ]; then
        echo "Fetching latest README.md from GitHub..."
        if curl -fsSL "$README_URL" -o "$README_FILE"; then
            echo "Downloaded README.md"
        else
            echo "Failed to download README.md"
            return 1
        fi
    elif [ ! -f "$README_FILE" ]; then
        echo "README.md not found, downloading..."
        if curl -fsSL "$README_URL" -o "$README_FILE"; then
            echo "Downloaded README.md"
        else
            echo "Failed to download README.md"
            return 1
        fi
    fi

    if [ -f "$README_FILE" ]; then
        bat --style=auto --paging=always "$README_FILE"
    else
        echo "README.md not found at $README_FILE"
        return 1
    fi
}
export -f helpf

# Optionally launch GUI on activation
if [ "${LMS_GUI}" = "true" ]; then
    lm-studio &
fi
'''

zsh = '''
# Wait for API server if services are starting
if [ "${FLOX_ACTIVATE_START_SERVICES:-}" = "true" ]; then
  printf "Waiting for LM Studio API"
  _lms_ready=false
  for _i in $(seq 1 30); do
    if curl -sf "http://${LMS_HOST}:${LMS_PORT}/v1/models" >/dev/null 2>&1; then
      _lms_ready=true
      break
    fi
    printf "."
    sleep 2
  done
  if [ "$_lms_ready" = "true" ]; then
    echo " ready!"
  else
    echo " not responding (check: flox services logs lm-studio)"
  fi
fi

helpf() {
    local README_FILE="$FLOX_ENV_PROJECT/README.md"
    local README_URL="https://raw.githubusercontent.com/floxrox/agentic-development-with-flox/main/agentic-lmstudio/README.md"

    if [ "$1" = "--help" ]; then
        echo "Usage: helpf [OPTIONS]"
        echo ""
        echo "View environment documentation"
        echo ""
        echo "Options:"
        echo "  --force    Force download fresh copy from GitHub"
        echo "  --help     Show this help message"
        echo ""
        echo "The README is cached locally and only downloaded if missing."
        return 0
    fi

    if [ "$1" = "--force" ]; then
        echo "Fetching latest README.md from GitHub..."
        if curl -fsSL "$README_URL" -o "$README_FILE"; then
            echo "Downloaded README.md"
        else
            echo "Failed to download README.md"
            return 1
        fi
    elif [ ! -f "$README_FILE" ]; then
        echo "README.md not found, downloading..."
        if curl -fsSL "$README_URL" -o "$README_FILE"; then
            echo "Downloaded README.md"
        else
            echo "Failed to download README.md"
            return 1
        fi
    fi

    if [ -f "$README_FILE" ]; then
        bat --style=auto --paging=always "$README_FILE"
    else
        echo "README.md not found at $README_FILE"
        return 1
    fi
}

# Optionally launch GUI on activation
if [ "${LMS_GUI}" = "true" ]; then
    lm-studio &
fi
'''

fish = '''
# Wait for API server if services are starting
if test "$FLOX_ACTIVATE_START_SERVICES" = "true"
  printf "Waiting for LM Studio API"
  set _lms_ready false
  for _i in (seq 1 30)
    if curl -sf "http://$LMS_HOST:$LMS_PORT/v1/models" >/dev/null 2>&1
      set _lms_ready true
      break
    end
    printf "."
    sleep 2
  end
  if test "$_lms_ready" = "true"
    echo " ready!"
  else
    echo " not responding (check: flox services logs lm-studio)"
  end
end

function helpf
    set README_FILE "$FLOX_ENV_PROJECT/README.md"
    set README_URL "https://raw.githubusercontent.com/floxrox/agentic-development-with-flox/main/agentic-lmstudio/README.md"

    if test "$argv[1]" = "--help"
        echo "Usage: helpf [OPTIONS]"
        echo ""
        echo "View environment documentation"
        echo ""
        echo "Options:"
        echo "  --force    Force download fresh copy from GitHub"
        echo "  --help     Show this help message"
        echo ""
        echo "The README is cached locally and only downloaded if missing."
        return 0
    end

    if test "$argv[1]" = "--force"
        echo "Fetching latest README.md from GitHub..."
        if curl -fsSL "$README_URL" -o "$README_FILE"
            echo "Downloaded README.md"
        else
            echo "Failed to download README.md"
            return 1
        end
    else if not test -f "$README_FILE"
        echo "README.md not found, downloading..."
        if curl -fsSL "$README_URL" -o "$README_FILE"
            echo "Downloaded README.md"
        else
            echo "Failed to download README.md"
            return 1
        end
    end

    if test -f "$README_FILE"
        bat --style=auto --paging=always "$README_FILE"
    else
        echo "README.md not found at $README_FILE"
        return 1
    end
end

# Optionally launch GUI on activation
if test "$LMS_GUI" = "true"
    lm-studio &
end
'''

[options]
systems = [
  "aarch64-darwin",
  "aarch64-linux",
  "x86_64-linux",
]
